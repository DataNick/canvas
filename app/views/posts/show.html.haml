%h1= @post.title
%p= @post.description
%p= @post.link

= link_to "Edit", edit_post_path(@post)
= link_to "Destroy", destroy_post_path(@post), method: :delete, data: {confirm: "Are you sure?"}
= link_to "Home", root_path 